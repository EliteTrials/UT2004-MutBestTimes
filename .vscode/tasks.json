{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile ServerBTimes",
            "type": "shell",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "windows": {
                "command": "${workspaceFolder}/make.bat"
            }
        }
    ]
}